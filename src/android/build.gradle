buildscript {
    repositories {
        mavenCentral()
    }
}
dependencies {
    // Facebook Core only (Analytics)
    compile 'com.facebook.android:facebook-core:5.0.0'
    
    // Facebook Android SDK (everything)
    compile 'com.facebook.android:facebook-android-sdk:5.0.0'
}
